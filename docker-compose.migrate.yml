services:
  migrate:
    image: python:3.11-alpine
    container_name: franco-migrate
    working_dir: /app
    volumes:
      - .:/app
    command: >
      sh -c "
      pip install --quiet beautifulsoup4 html2text PyYAML lxml &&
      python3 scripts/migrate_content.py
      "
